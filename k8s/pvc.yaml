apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: segmentation-db-pvc
  namespace: segmentation-mlops
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
