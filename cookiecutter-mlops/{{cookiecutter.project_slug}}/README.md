# {{ cookiecutter.project_title }}

Сгенерировано Cookiecutter из шаблона `cookiecutter-mlops`.

- Автор: {{ cookiecutter.author_name }}
- Python: {{ cookiecutter.python_version }}

## Дальнейшие шаги

1. `git init` и **Conventional Commits** (`feat:`, `fix:`, `chore:`).
2. `dvc init`, настроить `dvc remote`, `dvc repro`.
3. Заполнить `src/{{ cookiecutter.project_slug }}/` (или переименовать пакет) под вашу задачу.
4. Подключить MLflow, CI/CD, Kubernetes, Argo CD по образцу основного репозитория.
